No 26 issue, bug

for taring
	ignore directories that have no fastq.gz files in them

testing:

got a list of the tar files, push them to an array, then for each tar file

and mkdir test in each dir, compare files checksum?
or simple untar to /dev/null?
